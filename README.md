### .env
```.env
COMPOSE_PROJECT_NAME=qbittorrent
TIMEZONE=America/Toronto

DOCKER_NETWORK=<name of docker network>
IP_ADDRESS=<ip address of container>

# Update
DOWNLOADS=<path to mount>

SSL=<path to ssl>
NGINX=<path to nginx>

QBITTORRENT_CONFIG=<path to container config>
```
